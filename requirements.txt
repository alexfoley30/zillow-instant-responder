firebase-admin==7.5.0
anthropic>=0.116.0
# Pinned 2026-08-24 after a fresh Render rebuild went fail-closed with
# '400 Invalid database id %28default%29' on every Firestore call. Culprit
# isolated locally: google-api-core 2.35.0 percent-encodes the database
# resource path; 2.33.0 is the last known-good. firestore/firebase pinned
# alongside for deterministic rebuilds.
google-cloud-firestore==2.28.0
google-api-core==2.33.0
